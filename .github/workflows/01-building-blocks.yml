name: 01- Building Block

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
          - name: Say hello
            run: echo "Hello World!!!"
    
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
          - name: Bye
            run: "Bye!!!"