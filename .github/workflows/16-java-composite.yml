name: Java CI(Composite)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Java Composite Action
        uses: ./.github/actions/composite-cache-dependncies
